/**
 * @exercise "Escribiendo JSX"
 */

// Borra el comentario de la siguiente línea una vez agregado el JSX
// eslint-disable-next-line no-unused-vars
import React from 'react';

const Exercise = () => {
  // Cambia el valor de null por un JSX que desees ver en el navegador
  return null;
};


/**
 * No hacer cambios en las líneas inferiores. Lo que sigue es la configuración
 * para poder visualizar tus cambios en el formato del workshop.
 */

const Usage = () => {
  return <Exercise />;
};

Usage.title = 'Escribiendo JSX';

export default Usage;
